# Cloud Run environment variables for TraceFlow (Cloud SQL)
# Pass this file with: gcloud run deploy ... --env-vars-file .env.cloudsql

DB_ENGINE=django.db.backends.postgresql
DB_NAME=postgres
DB_USER=traceflow_user
DB_PASSWORD=5iwoxkMo

# Cloud Run connects via Unix domain socket
INSTANCE_CONNECTION_NAME=agorapolis:us-central1:traceflow-db
DB_PORT=5432

DEBUG=False
ALLOWED_HOSTS=*.run.app
